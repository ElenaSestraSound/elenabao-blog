import { NavMenuItemModel } from "@/lib/types";

export const MENU_ITEMS: NavMenuItemModel[] = [
    { name: "Home", to: '/' },
    { name: "Blog", to: '/posts' },
    { name: "Search", to: '/search' }
]